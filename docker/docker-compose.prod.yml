version: "3.9"

services:
  web:
    build:
      dockerfile: docker/web.Dockerfile
      target: prod
    ports: ["3000:3000"]
    restart: unless-stopped

  api:
    build:
      dockerfile: docker/api.Dockerfile
      target: prod
    ports: ["8000:8000"]
    restart: unless-stopped
